ngDescribe({
    name: 'Test comment-modal component',
    modules: 'app',
    element: '<comment-modal></comment-modal>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
