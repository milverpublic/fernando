ngDescribe({
    name: 'Test steeper-form component',
    modules: 'app',
    element: '<steeper-form></steeper-form>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
