ngDescribe({
    name: 'Test FunctionsService',
    modules: 'app',
    inject: 'FunctionsService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
